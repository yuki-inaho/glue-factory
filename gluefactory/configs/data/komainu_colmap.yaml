name: komainu_colmap
root: komainu_colmap
scene_list: ["."]  # Treat root as single scene
sfm: sparse/0
image_dir: images
views: views.txt
min_covisible_points: 10
min_overlap: 0.1
max_overlap: 0.9
max_per_scene: null  # null = use all pairs (237 pairs total)
preprocessing:
    resize: 640
    side: long
    square_pad: false
    antialias: false
num_workers: 2
seed: 42
overwrite: false  # Set true to regenerate views.txt and pairs
